name: Static checks

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm install --no-save linkedom
      - run: mkdir -p dist && cp index.html *.js *.css dist/ && cp -r data dist/data
      - run: node tests/tertiary.cjs
      - run: node tests/chemistry-editor.cjs
      - run: node tests/chemistry.cjs
      - run: node tests/secondary-v7.cjs
